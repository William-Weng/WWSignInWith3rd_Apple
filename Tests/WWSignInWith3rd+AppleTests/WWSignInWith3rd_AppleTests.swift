import XCTest
@testable import WWSignInWith3rd_Apple

final class WWSignInWith3rd_AppleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWSignInWith3rd_Apple().text, "Hello, World!")
    }
}
